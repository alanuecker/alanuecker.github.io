import styled from '@emotion/styled';

const H1 = styled.h1`
  font-weight: normal;
  letter-spacing: 0.25rem;
  text-transform: uppercase;
  font-size: 1.25rem;
  line-height: 1.5rem;
  margin: 0;
  white-space: pre-line;
  overflow: ellipsis;
`;

export default H1;
